<aura:component implements="force:hasRecordId,force:lightningQuickActionwithoutHeader" >
    <aura:html tag="style">
        .cuf-content{
            padding : 0 !important;
            margin : 0 !important;
        }
        .modal-body {
            padding : 0 !important;
            margin : 0 !important;
        }
    </aura:html>

    <c:callMe recordId = "{!v.recordId}" onclose="{!c.closeQA}"/>
</aura:component>