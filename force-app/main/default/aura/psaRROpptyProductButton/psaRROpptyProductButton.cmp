<aura:component implements="force:hasRecordId,force:lightningQuickActionwithoutHeader" >
    <aura:html tag="style">
        .cuf-content{
            padding : 0 !important;
            margin : 0 !important;
        }
        .modal-body {
            padding : 0 !important;
            margin : 0 !important;
        }
        .slds-modal__container {
            width : 80% !important;
            max-width : 80% !important;
        }
    </aura:html>
    <c:psaRROpptyProduct recordId="{!v.recordId}" onclose="{!c.closeQA}"/>
</aura:component>